# Local data workspace

This directory is ignored by default. Keep raw PDFs, Apple Health exports, FHIR/CDA/XML,
spreadsheets, and generated data stores out of Git unless explicitly de-identified and approved.
